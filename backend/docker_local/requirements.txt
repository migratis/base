django>=5.0.3
django-clearcache>=1.2.1
# Pinned to the major API the generator targets (modern django-ninja 1.x +
# pydantic 2.x: Meta/fields, check_fields=False). Floats to latest stable
# WITHIN the major; the <N caps stop a future major from silently breaking
# generated apps. See memory: project-generator-ninja-version-skew.
django-ninja>=1.1.0,<2
pydantic>=2.0,<3
psycopg2>=2.9.3
django-countries>=7.3.2
django-colorfield>=0.8.0
django-cors-headers>=3.11.0
django-crontab>=0.7.1
django-extensions>=3.1.5
django-import-export>=2.8.0
django-environ>=0.8.1
# Required by pydantic EmailStr (user/schemas.py).
email_validator>=2.1.1
# Not imported directly: runtime plugin of django-import-export/tablib for the
# admin's xlsx import/export format.
openpyxl>=3.0.10
# Generated app code emits `from PIL import Image` (image-field seed data).
pillow>=10.0.0
python-dateutil>=2.8.2
requests>=2.31.0
six>=1.16.0
stripe>=5.0.0
