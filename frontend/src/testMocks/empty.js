// Jest stub for side-effect-only ESM packages that Jest can't parse
// (e.g. `@geoman-io/leaflet-geoman-free`, imported only for its Leaflet
// augmentation). Mapped in via package.json "jest".moduleNameMapper.
module.exports = {};
